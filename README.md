# Swift-Python-C-OCR-
使用 Swift + AppKit 构建桌面应用：可拖拽图片、截图区域识别、显示识别文本并支持复制。Python 提供 OCR 服务（Flask 或直接子进程调用），接收图片路径或 base64，返回识别结果及置信度。C++ 封装图像预处理函数（灰度化、二值化、去噪），编译为动态库供 Python 调用。实现历史记录和导出 TXT。输出 Swift、Python、C++ 三端代码及集成说明。
